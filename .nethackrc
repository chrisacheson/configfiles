#
# System-wide NetHack configuration file for console-only NetHack.
#
OPTIONS=windowtype:tty,toptenwin,hilite_pet,hpmon,number_pad
OPTIONS=fixinv,safe_pet,sortpack,tombstone,color
OPTIONS=verbose,news,fruit:steak,DECgraphics,showexp,time,boulder:0
#OPTIONS=dogname:Slinky
#OPTIONS=catname:Rex
OPTIONS=catname:Cornelius
OPTIONS=pickup_types:$
OPTIONS=nomail
OPTIONS=msg_window:full,!legacy

#OPTIONS=role:wizard,race:elf,gender:female
OPTIONS=name:Kenshin,role:samurai,gender:male

# Enable this if you want to see your inventory sorted in alphabetical
# order by item instead of by index letter:
# OPTIONS=sortloot:full
# or if you just want containers sorted:
# OPTIONS=sortloot:loot

#
# Some sane defaults for menucolor
#

OPTIONS=menucolors
MENUCOLOR=" blessed "=green
MENUCOLOR=" holy "=green
MENUCOLOR=" uncursed "=yellow
MENUCOLOR=" cursed "=red
MENUCOLOR=" unholy "=red
MENUCOLOR=" cursed .* (being worn)"=orange&underline

MENUCOLOR=" wand of polymorph"=magenta
MENUCOLOR=" potion of polymorph"=magenta
