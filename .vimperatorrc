set complete=lf
set defsearch=
set focuscontent
set followhints=1
set hlsearch

" Need this in Firefox v29 to keep the navigation bar from appearing
set gui=nonavigation

" Show numbers on tabs for easier switching
set tabnumbers

set "titlestring=Mozilla Firefox"
